 使用自增主键后没当插入一条数据，新数据的主键id插在最后一条数据之后，
 不会频繁的断开数据之间已经建好的链表结构再重新建立链表。
 如果使用自定义的主键id，每一次可能都需要先找到要存放的页，然后再频繁的断开链表，指向新链表

 使用自定义主键可能会发生已经有id = 1，2，3，4，5，7，8，9八条数据填满一页了，这时候执行了一次插入id = 6的数据
 这就需要破坏掉原有的结构，多余出来id = 9的数据去填充到新开的一页中