# 网络
cd /etc/sysconfig/network-scripts/ifcfg-eth0
设置ONBOOT=YES
重启network
service network restart

# 查看ip地址
ip addr

# 查看当前文件夹 ll
drwxr-xr-x 2 root root         6 2月   9 13:34 my.cnf
· d 目录 i 软连接 - 文件 c 字符设备文件
· 三组rwx u 所有者 g 所属组 o 其他人  r 读 w 写 x 执行
· 硬连接引用次数
· 文件拥有者
· 文件所属组
· 占用字节
· 最后修改时间
· 文件名

# 查看端口使用情况
netstat -ntulp | grep 端口号

# 结合命令使用直接获取当前端口号运行的程序的进程id
netstat -nlp | grep :${port} | awk '{print $7}' | awk -F"/" '{ print $1 }'

# 文件授权 u 自己 g 所属组 o 其他人 r 读 w 写 x 执行
chmod ugo+wrx restart.sh

# 更改文件所有者 -R穿透
chown -R user1:user1 my.txt

# 查看进程 ps
-aux 显示所有包含其他使用者的进程
-ef 差不多意思

# 查看文件 tail
-f 循环读取
-n<行数> 显示文件的尾部 n 行内容

# vi/vim
查找 :/<查找内容>  N 向前查询
行号 :set nu
控制字符 :set list
另存为 :w 路径


